<div class="mmooc-enroll-category">
    <!-- TODO: remove when the work on new features is finalised - I left it as a point of reference while we work on new features
    <h2 class="mmooc-enroll-category-title">{{title}}</h2>
    <ul>
        {{#each courses}}
        <li class="{{#if enrolled}}enrolled{{else}}notenrolled{{/if}}">
            {{#if enrolled}}
            <a href="{{urlForCourseId id}}">
                {{else}}
                <a class="all-courses-show-modal" href="#">
                    {{/if}} {{name}}
                </a>
        </li>
        {{#unless enrolled}}
        <div class="all-courses-modal modal-course-{{id}}">
            <div class="modal-content">
                <div class="modal-header">
                    <div class="modal-close modal-back"></div>
                </div>
                <div class="modal-description">
                    <h3>{{name}}</h3>
                    <div>{{public_description}}</div>
                </div>
                <div class="modal-footer">
                    <a class="modal-back" href="#">Tilbake til {{courseLabel}}oversikten</a>
                    {{#if self_enrollment_code}}
                    <a class="btn modal-enroll" href="/enroll/{{self_enrollment_code}}">Meld deg på</a>{{/if}}
                </div>
            </div>
        </div>
        {{/unless}} {{/each}}
    </ul>
    -->
    <section class="mmooc-accordion">
        <button id="mmooc-accordion-header-{{index}}" class="mmooc-accordion-header" index={{index}}>
            <div class="mmooc-accordion-header-main" index={{index}}>
                <div>
                    <h1 index={{index}}>{{title}}</h1>
                </div>
                <div>
                    <!-- TODO: variable with a number of courses shall be placed here -->
                    <div class="mmooc-tag mmooc-tag-teal">
                        <label index={{index}}>1 kurs</label>
                    </div>
                    <!-- TODO: variable with a number of courses the user is subscribed to shall be placed here
                         also, it should be visible only on the 'logged in screen' -->
                    <div class="mmooc-tag mmooc-tag-green">
                        <label index={{index}}>du er registerd in 1 kurs</label>
                    </div>
                </div>
            </div>
            <div class="mmooc-accordion-label-closed">
                <label class="mmooc-accordion-header-label" index={{index}}>Utsikt</label>
                <svg width="16" height="9" xmlns="http://www.w3.org/2000/svg" index={{index}}>
                    <g fill="none" fill-rule="evenodd" index={{index}}>
                        <path d="M-8-12h32v32H-8z" />
                        <path fill-opacity=".5" fill="#373737" d="M1.887 0L8 5.565 14.113 0 16 1.717 8 9 0 1.717z" />
                    </g>
                </svg>
            </div>
            <div class="mmooc-accordion-label-open">
                <label class="mmooc-accordion-header-label" index={{index}}>Lukke</label>
                <svg width="16" height="9" xmlns="http://www.w3.org/2000/svg" index={{index}}>
                    <g fill="none" fill-rule="evenodd" index={{index}}>
                        <path d="M-8 21h32v-32H-8z" />
                        <path fill="#24A1B3" d="M1.887 9L8 3.435 14.113 9 16 7.283 8 0 0 7.283z" />
                    </g>
                </svg>
            </div>
        </button>
        <article id="mmooc-accordion-content-{{index}}" class="mmooc-accordion-content">
            <div class="mmooc-tabs">
                <div class="mmooc-tab-heads">
                    {{#each courses}}
                    <button id="mmooc-tab-head-{{id}}" class="mmooc-tab-head mmooc-tab-head-{{@index}}" index="{{id}}">{{name}}</button>
                    {{/each}}
                </div>
                <div class="mmooc-tab-content-ctr">
                    {{#each courses}}
                    <div id="mmooc-tab-content-{{id}}" class="mmooc-tab-content mmooc-tab-content-{{@index}}">
                        <p>{{public_description}}</p>
                        <div class="mmooc-button-ctr">
                            <button class="mmooc-button mmooc-button-primary">Registrere med Feide</button>
                            <button class="mmooc-button mmooc-button-secondary">Registrere</button>
                        </div>
                    </div>
                    {{/each}}
                </div>
            </div>
        </article>
    </section>
</div>